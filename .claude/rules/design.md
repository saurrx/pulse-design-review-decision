---
paths:
  - "src/components/**"
  - "src/pages/**"
  - "src/index.css"
  - "src/style.css"
  - "src/styles/**"
---
You are editing V0 product files. Restyle and restructure in place through
tokens and the existing primitives; keep the route, the data hooks and the
adapter. If the screen needs a route, field or state the mock lacks, add it
to the mock and declare it in mock/proposed-routes.json or
mock/proposed-fields.json — never fake it in the component. Match the
reference screens (Workspace Admin Overview and Ideas queue). Check the
surface brief and design/v0/COGNITIVE-LOAD.md before adding any element.
