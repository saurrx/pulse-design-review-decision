---
paths:
  - "src/components/**"
  - "src/pages/**"
  - "src/index.css"
  - "src/style.css"
  - "src/styles/**"
---
You are editing portable product files. They travel to production as a patch.
Restyle in place through tokens and the existing primitives. Do not change API
paths, request bodies, query keys, navigation targets, role or status literals,
or storage keys here; if a change needs one, say so in the record, because the
exporter will escalate it to behaviour-impact approval.
