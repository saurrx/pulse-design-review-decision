# Empty in dev: vite.config proxies /v1 to the API on the same origin, which
# keeps the API's HttpOnly cookies working without SameSite friction.
VITE_API_URL=


# Must match GOOGLE_CLIENT_ID on the API — the token audience is verified there.
VITE_GOOGLE_CLIENT_ID=
