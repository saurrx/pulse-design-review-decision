import type { Config } from "tailwindcss";
import { colors as tokens, fontFamily as tokenFonts, radius as tokenRadius } from "./src/styles/tokens.tailwind";

export default {
  darkMode: ["class"],
  content: [
    "./pages/**/*.{ts,tsx}",
    "./components/**/*.{ts,tsx}",
    "./app/**/*.{ts,tsx}",
    "./src/**/*.{ts,tsx}",
  ],
  prefix: "",
  theme: {
    // DSN-0003: the radius scale replaces Tailwind's, so `rounded-xs|sm|md|lg|full`
    // are the only corner utilities and each resolves to its token variable.
    // `rounded`, `rounded-xl` and arbitrary values do not exist; qa/v0/radius.test.ts
    // refuses them.
    borderRadius: {
      none: "0px",
      xs: `var(--pl-radius-xs, ${tokenRadius.xs})`,
      sm: `var(--pl-radius-sm, ${tokenRadius.sm})`,
      md: `var(--pl-radius-md, ${tokenRadius.md})`,
      lg: `var(--pl-radius-lg, ${tokenRadius.lg})`,
      full: tokenRadius.full,
    },
    container: {
      center: true,
      padding: "2rem",
      screens: {
        "2xl": "1400px",
      },
    },
    extend: {
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
        sidebar: {
          DEFAULT: "hsl(var(--sidebar-background))",
          foreground: "hsl(var(--sidebar-foreground))",
          primary: "hsl(var(--sidebar-primary))",
          "primary-foreground": "hsl(var(--sidebar-primary-foreground))",
          accent: "hsl(var(--sidebar-accent))",
          "accent-foreground": "hsl(var(--sidebar-accent-foreground))",
          border: "hsl(var(--sidebar-border))",
          ring: "hsl(var(--sidebar-ring))",
        },
        // Pulse Design Tokens (PL-TKN-004): generated by tools/tokens.mjs from src/styles/tokens.json
        photon: tokens.photon,
        pl: tokens.pl,
      },
      keyframes: {
        "accordion-down": {
          from: { height: "0" },
          to: { height: "var(--radix-accordion-content-height)" },
        },
        "accordion-up": {
          from: { height: "var(--radix-accordion-content-height)" },
          to: { height: "0" },
        },
        "fade-in": {
          "0%": { opacity: "0", transform: "translateY(5px)" },
          "100%": { opacity: "1", transform: "translateY(0)" },
        },
        "fade-in-right": {
          "0%": { opacity: "0", transform: "translateX(10px)" },
          "100%": { opacity: "1", transform: "translateX(0)" },
        },
        "fade-out": {
          "0%": { opacity: "1" },
          "100%": { opacity: "0" },
        },
        "scale-in": {
          "0%": { opacity: "0", transform: "scale(0.97)" },
          "100%": { opacity: "1", transform: "scale(1)" },
        },
        "spin-slow": {
          "0%": { transform: "rotate(0deg)" },
          "100%": { transform: "rotate(360deg)" },
        },
        shimmer: {
          "100%": { transform: "translateX(100%)" },
        },
        borderShine: {
          '0%': { transform: 'translateX(-100%)' },
          '100%': { transform: 'translateX(600%)' },
        },
      },
      animation: {
        "accordion-down": "accordion-down 0s ease-out",
        "accordion-up": "accordion-up 0s ease-out",
        "fade-in": "fade-in 0s ease-out",
        "fade-in-right": "fade-in-right 0s ease-out",
        "fade-out": "fade-out 0s ease-out",
        "scale-in": "scale-in 0s ease-out",
        "spin-slow": "spin-slow 20s linear infinite",
        'border-shine': 'borderShine 3.5s linear infinite',
      },
      // One calm product face across the entire application. Monospace is
      // reserved for identifiers and provenance records only.
      // The sheet's three families (generated): Schibsted Grotesk for the interface, Newsreader for headlines and
      // human prose, IBM Plex Mono for reference numbers, dates and citations only.
      fontFamily: tokenFonts,
      fontSize: {
        xs: ["13px", { lineHeight: "18px" }],
        sm: ["14px", { lineHeight: "20px" }],
        base: ["15px", { lineHeight: "22px" }],
        lg: ["18px", { lineHeight: "26px" }],
        xl: ["22px", { lineHeight: "28px" }],
        "2xl": ["28px", { lineHeight: "34px" }],
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
} satisfies Config;
