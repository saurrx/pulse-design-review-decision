# Our app, served by vite dev so the /v1 proxy behaves as in development.
FROM node:22-slim
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --no-audit --no-fund
COPY . .
EXPOSE 3600
CMD ["npx","vite","--host","0.0.0.0","--port","3600"]
